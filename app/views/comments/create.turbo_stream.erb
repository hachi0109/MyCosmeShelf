<%= turbo_stream.remove "no-comments-msg" %>
<%= turbo_stream.prepend "comments-list", partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.replace "comment-form", partial: "comments/form", locals: { cosmetic: @cosmetic, comment: Comment.new } %>
